# FreezR Security Architecture

## 🔒 Принципы безопасности

FreezR разработан с учётом принципа **наименьших привилегий** и современных практик безопасности Linux.

### ❌ Что FreezR НЕ использует

- ❌ **Sudo** - нигде в коде нет вызовов sudo
- ❌ **SUID binaries** - нет повышения привилегий через файловую систему
- ❌ **Root user** - работает от обычного пользователя

### ✅ Что FreezR использует

- ✅ **Linux Capabilities** - точечные привилегии вместо root
- ✅ **D-Bus API** - стандартный способ управления systemd
- ✅ **Polkit** - контролируемая авторизация операций
- ✅ **NoNewPrivileges** - защита от повышения привилегий

## 🛡️ Capabilities

FreezR требует только 2 capabilities:

### CAP_KILL
**Для чего:** Отправка сигналов процессам (SIGTERM, SIGKILL, SIGSTOP, SIGCONT)

**Используется в:**
- Убийство процессов Node.js при превышении CPU
- Заморозка/разморозка браузеров (Firefox, Brave)
- Убийство Telegram при критической нагрузке

**Безопасность:** Ограничено процессами пользователя ryazanov

### CAP_SYS_NICE
**Для чего:** Изменение приоритета процессов (nice level)

**Используется в:**
- Понижение приоритета Snap процессов при высокой нагрузке

**Безопасность:** Может только понижать приоритет (повышать nice value)

## 🚪 D-Bus и Polkit

### Управление systemd без sudo

FreezR использует **D-Bus API** для перезапуска KESL службы:

```rust
// Вместо: sudo systemctl restart kesl
// Используем: D-Bus RestartUnit
proxy.call_method("RestartUnit", &("kesl.service", "replace"))
```

### Polkit авторизация

Права контролируются через polkit правила:

**Файл:** `/etc/polkit-1/rules.d/50-freezr-kesl-restart.rules`

```javascript
polkit.addRule(function(action, subject) {
    if (action.id == "org.freedesktop.systemd1.manage-units") {
        if (subject.user == "ryazanov") {
            var verb = action.lookup("verb");
            var unit = action.lookup("unit");

            // Разрешить только daemon-reload
            if (verb == "reload") {
                return polkit.Result.YES;
            }

            // Разрешить только restart kesl.service
            if (verb == "restart" && unit == "kesl.service") {
                return polkit.Result.YES;
            }
        }
    }
});
```

**Безопасность:**
- ✅ Только конкретный пользователь (ryazanov)
- ✅ Только конкретная служба (kesl.service)
- ✅ Только конкретные операции (reload, restart)
- ✅ Логирование всех операций в polkit logs

## 🔐 Systemd Security Hardening

Служба FreezR использует строгие ограничения:

```ini
[Service]
# Запрет повышения привилегий
NoNewPrivileges=true

# Изоляция файловой системы
ProtectSystem=strict
ProtectHome=read-only
PrivateTmp=true

# Только необходимые capabilities
AmbientCapabilities=CAP_SYS_NICE CAP_KILL
CapabilityBoundingSet=CAP_SYS_NICE CAP_KILL CAP_DAC_OVERRIDE

# Ограничение ресурсов для самого монитора
CPUQuota=5%
MemoryMax=50M
```

### Что это даёт

1. **NoNewPrivileges=true**
   - Процесс не может повысить свои привилегии
   - Sudo не будет работать (и не нужен!)
   - Защита от эксплойтов с повышением привилегий

2. **ProtectSystem=strict**
   - Файловая система только для чтения
   - Записывать можно только в явно разрешённые директории
   - Защита от изменения системных файлов

3. **Ограниченные Capabilities**
   - Только CAP_KILL и CAP_SYS_NICE
   - Нет доступа к сети, сырым сокетам, модулям ядра
   - Минимум привилегий для работы

## 🔍 Audit и Мониторинг

### Логирование всех действий

FreezR логирует все операции:

```
[INFO] Restarting KESL service with daemon-reload
[INFO] KESL service successfully restarted
[INFO] Killed Node.js process PID 12345 (CPU: 150%)
[INFO] Frozen Firefox process PID 67890 (CPU: 85%)
[INFO] Applied nice level 15 to Snap process PID 54321
```

### Проверка безопасности

```bash
# 1. Проверить capabilities процесса
cat /proc/$(pgrep process-monitor)/status | grep Cap
# CapEff должен содержать только CAP_KILL и CAP_SYS_NICE

# 2. Проверить NoNewPrivileges
cat /proc/$(pgrep process-monitor)/status | grep NoNewPrivs
# NoNewPrivs: 1 (включено)

# 3. Проверить что sudo не работает
# Попытка sudo из процесса вернёт ошибку:
# "sudo: The 'no new privileges' flag is set"

# 4. Проверить polkit логи
sudo journalctl -u polkit | grep freezr
# Должны видеть разрешённые операции
```

## 🎯 Принцип наименьших привилегий

FreezR следует принципу **least privilege**:

| Операция | Требуемые права | Почему минимальны |
|----------|----------------|-------------------|
| Сканирование процессов | Нет | ps/top - обычные команды |
| Kill процесса | CAP_KILL | Только для процессов пользователя |
| Nice процесса | CAP_SYS_NICE | Только понижение приоритета |
| Restart службы | Polkit rule | Только конкретная служба |
| Чтение конфига | Read access | Только свои файлы |
| Запись логов | Write access | Только свой log dir |

**Сравнение с root:**
- Root может: **ВСЁ** (полный доступ к системе)
- FreezR может: Только kill/nice процессов + restart одной службы

## 🚨 Потенциальные риски

### Риск 1: Abuse of CAP_KILL
**Сценарий:** Злоумышленник получил доступ к процессу FreezR

**Последствия:**
- ❌ Может убить процессы пользователя ryazanov
- ✅ НЕ может убить процессы других пользователей
- ✅ НЕ может убить системные процессы (root)
- ✅ НЕ может получить root доступ

**Миtigация:**
- ProtectHome и ProtectSystem ограничивают доступ к файлам
- NoNewPrivileges предотвращает эскалацию
- Все действия логируются

### Риск 2: Abuse of D-Bus restart
**Сценарий:** Злоумышленник пытается перезапускать KESL постоянно

**Последствия:**
- ❌ Может перезапускать только kesl.service
- ✅ НЕ может перезапускать другие службы
- ✅ НЕ может останавливать службы
- ✅ НЕ может изменять конфигурацию

**Миtigация:**
- Polkit ограничивает только restart kesl.service
- FreezR имеет защиту от частых рестартов (min 100 сек между рестартами)
- Все операции логируются в polkit и journalctl

### Риск 3: Denial of Service
**Сценарий:** FreezR сам потребляет много ресурсов

**Последствия:**
- ✅ Ограничено CPUQuota=5% и MemoryMax=50M
- ✅ Systemd автоматически убьёт процесс при превышении

**Миtigация:**
- Жёсткие лимиты в service файле
- Мониторинг собственного потребления ресурсов

## ✅ Соответствие best practices

FreezR следует современным практикам безопасности Linux:

- ✅ **Принцип наименьших привилегий** - только необходимые capabilities
- ✅ **Defense in depth** - множество уровней защиты
- ✅ **Secure by default** - безопасная конфигурация из коробки
- ✅ **Audit trail** - логирование всех операций
- ✅ **Fail secure** - при ошибках не даёт больше прав
- ✅ **No sudo** - нет shell команд с повышением привилегий
- ✅ **Systemd hardening** - использует все возможности systemd для изоляции

## 📚 Дополнительное чтение

- [Linux Capabilities man page](https://man7.org/linux/man-pages/man7/capabilities.7.html)
- [Systemd Security Features](https://www.freedesktop.org/software/systemd/man/systemd.exec.html#Security)
- [Polkit Documentation](https://www.freedesktop.org/software/polkit/docs/latest/)
- [D-Bus Security Model](https://dbus.freedesktop.org/doc/dbus-specification.html#auth-mechanisms)

## 🔄 Версии и изменения безопасности

### v0.1.0 (2025-10-27)
- ✅ Удалён весь код с sudo
- ✅ Добавлена D-Bus интеграция для systemd
- ✅ Переписан renice на прямой setpriority()
- ✅ Добавлены polkit правила
- ✅ Включен NoNewPrivileges в service файле
- ✅ Документирована архитектура безопасности

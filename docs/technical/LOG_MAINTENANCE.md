# FreezR Log Maintenance System

## 📋 Обзор

FreezR использует полный цикл логирования с автоматической ротацией, архивацией и очисткой.

## 🏗️ Архитектура логирования

### Компоненты

1. **tracing-appender** - Автоматическая ротация (новый файл каждый день)
2. **log_maintenance.sh** - Архивация и очистка старых логов
3. **Cron job** - Автоматическое выполнение обслуживания

### Структура директорий

```
freezr/
└── logs/
    ├── process_monitor.log.YYYY-MM-DD    # Активные логи (текущие)
    ├── freezr-daemon.log.YYYY-MM-DD      # Активные логи (текущие)
    └── archive/
        ├── process_monitor.log.YYYY-MM-DD.gz  # Сжатые архивы
        └── freezr-daemon.log.YYYY-MM-DD.gz    # Сжатые архивы
```

## ⚙️ Конфигурация

### Параметры хранения

| Параметр | Значение | Описание |
|----------|----------|----------|
| **DAYS_TO_KEEP_ACTIVE** | 7 дней | Сколько дней хранить активные логи |
| **DAYS_TO_KEEP_ARCHIVE** | 30 дней | Сколько дней хранить архивы |
| **Rotation** | DAILY | Новый файл каждый день в полночь |

### Изменить параметры

Отредактируйте `/home/ryazanov/.myBashScripts/freezr/scripts/log_maintenance.sh`:

```bash
DAYS_TO_KEEP_ACTIVE=7        # Сколько дней хранить активные логи
DAYS_TO_KEEP_ARCHIVE=30      # Сколько дней хранить сжатые архивы
```

## 🔄 Цикл жизни лога

```
День 0-7: Активный лог (logs/process_monitor.log.2025-10-26)
          ↓ (через 7 дней)
День 8:   Архивация и сжатие → logs/archive/process_monitor.log.2025-10-26.gz
          ↓ (ещё 23 дня)
День 31:  Удаление архива
```

## 📝 Использование

### Ручное выполнение

```bash
cd /home/ryazanov/.myBashScripts/freezr

# Полное обслуживание (архивация + очистка)
./scripts/log_maintenance.sh

# Только архивация старых логов
./scripts/log_maintenance.sh archive

# Только удаление старых архивов
./scripts/log_maintenance.sh clean

# Показать список всех логов
./scripts/log_maintenance.sh list

# Показать статистику
./scripts/log_maintenance.sh stats

# Справка
./scripts/log_maintenance.sh help
```

### Автоматическое выполнение (Cron)

**Установить cron задачи:**

```bash
./scripts/setup_log_cron.sh
```

**Расписание по умолчанию:**
- **Ежедневно в 3:00** - полное обслуживание (архивация + очистка)
- **Понедельник в 4:00** - детальный отчёт со списком файлов

**Проверить задачи:**

```bash
crontab -l | grep FreezR
```

**Лог cron:**

```bash
tail -f logs/log_maintenance_cron.log
```

## 📊 Статистика и мониторинг

### Текущее состояние

```bash
./scripts/log_maintenance.sh stats
```

**Вывод:**
```
📊 Статистика логов:
  Активные логи: 4 файлов (1.1M)
  Архивные логи: 5 файлов (250K)
```

### Список файлов

```bash
./scripts/log_maintenance.sh list
```

**Вывод:**
```
Активные логи (logs/):
  process_monitor.log.2025-10-26 - 50K (окт 26)
  process_monitor.log.2025-10-25 - 314K (окт 25)
  ...

Архивные логи (logs/archive/):
  process_monitor.log.2025-10-20.gz - 125K (окт 20)
  ...
```

## 🗜️ Сжатие

Архивы сжимаются с помощью `gzip`:

- **Коэффициент сжатия:** ~70-90% (в зависимости от содержимого)
- **Пример:** 500KB → 50-150KB

## 🎯 Преимущества

1. **Автоматическая ротация** - новый файл каждый день
2. **Сжатие архивов** - экономия места на диске (70-90%)
3. **Автоочистка** - старые логи удаляются автоматически
4. **Прозрачность** - все действия логируются
5. **Гибкость** - легко настроить периоды хранения
6. **Безопасность** - архивы не удаляются случайно (30 дней хранения)
7. **Интеграция с FreezR** - статистика логов отображается в live dashboard процесса-монитора

## 📈 Проекция роста

**При текущей нагрузке (~500KB/день):**

- **Активные логи (7 дней):** ~3.5MB
- **Архивы (30 дней):** ~3-5MB (сжатые)
- **Итого:** ~6-8MB максимум

**После автоочистки стабилизируется на уровне 6-8MB.**

## 🔧 Устранение проблем

### Логи не архивируются

1. Проверить права доступа:
   ```bash
   ls -ld logs/ logs/archive/
   ```

2. Проверить возраст файлов:
   ```bash
   find logs/ -name "*.log.*" -mtime +7 -ls
   ```

3. Запустить вручную с отладкой:
   ```bash
   bash -x ./scripts/log_maintenance.sh archive
   ```

### Cron не выполняется

1. Проверить задачи:
   ```bash
   crontab -l
   ```

2. Проверить лог cron:
   ```bash
   tail -50 logs/log_maintenance_cron.log
   ```

3. Проверить системный лог cron:
   ```bash
   grep CRON /var/log/syslog | tail -20
   ```

### Нет места на диске

Экстренная очистка всех старых логов:

```bash
# ВНИМАНИЕ: Удалит ВСЕ логи старше 3 дней!
find logs/ -name "*.log.*" -mtime +3 -delete
find logs/archive/ -name "*.gz" -mtime +3 -delete
```

## 📝 Примеры вывода

### Успешная архивация

```
[2025-10-26 13:22:42] INFO: 🗜️  Архивация старых логов (старше 7 дней)...
[2025-10-26 13:22:42] INFO:   ✓ process_monitor.log.2025-10-15 → process_monitor.log.2025-10-15.gz (сэкономлено: 450KB)
[2025-10-26 13:22:42] INFO:   ✓ freezr-daemon.log.2025-10-15 → freezr-daemon.log.2025-10-15.gz (сэкономлено: 380KB)
[2025-10-26 13:22:42] INFO:   ✓ Заархивировано: 2 файлов
[2025-10-26 13:22:42] INFO:   ✓ Сэкономлено места: 830KB
```

### Успешная очистка

```
[2025-10-26 13:22:45] INFO: 🗑️  Удаление старых архивов (старше 30 дней)...
[2025-10-26 13:22:45] INFO:   ✓ Удалён: process_monitor.log.2025-09-15.gz (125KB)
[2025-10-26 13:22:45] INFO:   ✓ Удалён: freezr-daemon.log.2025-09-15.gz (98KB)
[2025-10-26 13:22:45] INFO:   ✓ Удалено: 2 архивов
[2025-10-26 13:22:45] INFO:   ✓ Освобождено места: 223KB
```

## 📊 Интеграция с FreezR Dashboard

FreezR Process Monitor автоматически отображает статистику логов в реальном времени!

**В режиме `keslwatchR` вы увидите:**

```
╔═══════════════════════════════════════════════════════════╗
║                    Log Statistics                         ║
╚═══════════════════════════════════════════════════════════╝
   📄 Active logs: 4 files (1.1M)
   🗜️  Archive logs: 0 files (4.0K)
   📊 Retention: 7 days active, 30 days archived
```

**Преимущества интеграции:**
- Мониторинг в реальном времени - статистика обновляется автоматически
- Нет необходимости запускать отдельные команды
- Полная картина состояния системы в одном месте
- Видно сколько места занимают активные логи и архивы

**Как это работает:**
- FreezR автоматически сканирует директории `logs/` и `logs/archive/`
- Подсчитывает количество файлов логов и их общий размер
- Отображает информацию в секции "Log Statistics" dashboard
- Обновление происходит каждый интервал отчёта (по умолчанию 60 секунд)

## 🔗 Связанные файлы

- **Скрипт обслуживания:** `/home/ryazanov/.myBashScripts/freezr/scripts/log_maintenance.sh`
- **Настройка cron:** `/home/ryazanov/.myBashScripts/freezr/scripts/setup_log_cron.sh`
- **Логи cron:** `/home/ryazanov/.myBashScripts/freezr/logs/log_maintenance_cron.log`
- **Конфигурация логирования:** `crates/freezr-daemon/src/bin/process_monitor.rs:461-485`
- **Функция статистики логов:** `crates/freezr-daemon/src/bin/process_monitor.rs:212-261`
- **Dashboard интеграция:** `crates/freezr-daemon/src/bin/process_monitor.rs:452-463`

## ✅ Рекомендации

1. **Используйте keslwatchR** - статистика логов всегда под рукой в dashboard
2. **Запускайте cron задачи** - автоматизация важна для long-term maintenance
3. **Проверяйте логи периодически** - `./scripts/log_maintenance.sh stats` или просто смотрите в dashboard
4. **Настройте периоды хранения** под ваши нужды (7/30 дней по умолчанию)
5. **Мониторьте место на диске** - видно сразу в секции "Log Statistics"
6. **Делайте бэкапы важных логов** перед очисткой
